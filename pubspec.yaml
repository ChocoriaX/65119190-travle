name: travle_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.4
  http: ^0.13.3
  # path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # provider: ^6.1.2
  
flutter:
  uses-material-design: true
  assets:
    - assets/places.json
    - assets/data.json
    - assets/images/